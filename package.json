{
  "name": "script",
  "license": "MIT",
  "version": "0.0.1",
  "devDependencies": {
    "@prisma/cli": "2.0.0",
    "ts-node": "8.7.0",
    "typescript": "3.8.3"
  },
  "scripts": {
    "dev": "nexus dev",
    "build": "nexus build",
    "prisma-script": "ts-node ./script.ts",
    "test": "jest"
  },
  "dependencies": {
    "@prisma/client": "2.0.0",
    "nexus": "^0.24.2"
  },
  "engines": {
    "node": ">=10.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
