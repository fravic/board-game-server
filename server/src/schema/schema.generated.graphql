### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Game implements Node {
  id: ID!
  name: String!
  numPlayers: Int!
  players: [Player!]!
}

type GameAndPlayer {
  game: Game!
  player: Player!
}

type GameEvent {
  changedNodes: [Node!]!
}

type Mutation {
  createGame(name: String): Game!

  """
  Creates a new player, adds the player to a game, and sets the player cookie.
  """
  joinGameAsPlayer(gameId: ID!, name: String!): GameAndPlayer!
  playerHeartbeat(gameId: ID!, playerId: ID!): Game!
  startGame(gameId: ID!): Game!
}

interface Node {
  id: ID!
}

type Player implements Node {
  id: ID!
  name: String!
}

type Query {
  game(id: ID!): Game!
}

type Subscription {
  gameEvents(gameId: ID!): GameEvent!
}
